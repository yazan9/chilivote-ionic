import { ConnectionsSearchPipe } from './connections-search.pipe';

describe('ConnectionsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ConnectionsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
